# Token Plan

## Working Default

- Project: BagSplit Checkout
- Token Name: BagSplit Pay
- Ticker: BSPAY
- Status: Default assumed available until final Bags launch checks.

## Availability Note

Solana token tickers are not globally unique in the same way domain names are. I cannot definitively prove `BSPAY` is available by querying a universal registry. Per the current project decision, `BSPAY` remains the working default unless a conflict appears during the Bags launch flow.

## Launch Intent

BagSplit Pay is positioned as the demo creator token for checkout payments. It should be used to show how a creator token can unlock:

- Digital drops
- Membership passes
- Event tickets
- Fan services

## Pre-Submission Checks

- Confirm `BagSplit Pay` and `BSPAY` in the Bags token launch UI.
- Capture token launch screenshots.
- Record token mint address after launch.
- Update `.env.local` or project config with the final token mint.